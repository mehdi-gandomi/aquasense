# MQTT broker for live ingestion. MySQL is the operator's local instance.
services:
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: aquasense-mosquitto
    ports:
      - '1883:1883'
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
    restart: unless-stopped
